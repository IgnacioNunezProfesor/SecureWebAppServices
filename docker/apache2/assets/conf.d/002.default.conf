# DocumentRoot
DocumentRoot "${HTTP_APACHE2_SERVER_DEFAULT_FOLDER}"

<Directory "${HTTP_APACHE2_SERVER_DEFAULT_FOLDER}">
    Options -Indexes +FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>